<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/baseScreen">

    <fragment
        android:id="@+id/baseScreen"
        android:name="com.example.navigationbug.BaseScreen"
        android:label="fragment_base_screen"
        tools:layout="@layout/fragment_base_screen" />

    <action
        android:id="@+id/action_global_baseFragment"
        app:destination="@id/baseScreen"
        app:launchSingleTop="true"
        app:popUpTo="@+id/baseScreen"
        app:popUpToInclusive="true" />
</navigation>